[build-system]
requires = [
    "setuptools",
    "wheel",
    "Cython",
    "numpy",
    "scipy"
]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
